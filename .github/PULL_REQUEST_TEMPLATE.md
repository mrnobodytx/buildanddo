## BuildAndDo change

**Feature / mission ID:**  
**Actor type:** `human` / `agent` / `mixed`  
**Agent ID (if applicable):**  
**Risk / authority:** `A0` / `A1` / `A2` / proposed `A3`  
**Acceptance evidence:**  

### What changed

### Why it changed

### Verification performed

### Rollback / compensation

### Public/private boundary

- [ ] No golden, infrastructure, secret, private evidence, or deployment-control files are included.
- [ ] Exactly one PR label is applied: `actor:human`, `actor:agent`, or `actor:mixed`.
- [ ] The change is safe to expose publicly.
