# ─── CGRF Header ───────────────────────────────────────────────
# File:        .bits/handoffs/2026-09-21-bits-codegen-cmax-b-evolution-runtime.md
# Stage:       11_COMMIT
# SRS:         SRS-BUILDANDDO-EVOLUTION-001
# CAPS:        pending
# CK:          pending
# Dispatch:    VCC-BUILDANDDO-EVOLUTION-001
# Seat:        BITS-CODEGEN
# Owner:       Citadel Nexus Inc.
# Created:     2026-09-21
# Depends:     docs/verified-evolution.md, .bits/srs/SRS-BUILDANDDO-EVOLUTION-001.md
# EnumType:    Doc
# EnumEdges:   CONSUMES docs/verified-evolution.md; DEPENDS_ON .bits/srs/SRS-BUILDANDDO-EVOLUTION-001.md
# Intent:      Keep authenticated capture, model binding and governed execution with the receiving runtime owner.
# ───────────────────────────────────────────────────────────────

# Evolution runtime handoff

Dispatch: VCC-BUILDANDDO-EVOLUTION-001
From: BITS-CODEGEN
Receiving owner: CMAX-B, with IDE1 for mission hooks and USO_MCP for dispatch scope
Status: source interface ready; runtime activation not performed

The additive local fabric is callable through `scripts/citadel-evolve` and
`libs.evolution`. It retains immutable observations, independent outcome
receipts, separate discovery/replay/shadow windows, typed competence promotion,
proposal-only rules and demotion. Its SQLite scope partitions do not authenticate
exported identities or replace native tenant authorization.

## Receiving work

1. Authorize the runtime integration separately. Bind each permitted exporter to
   the authenticated account, tenant/workspace, actor and exact source revision.
   Keep private exports and credentials on the private plane.
2. After existing mission completion and independent TEVV, export the full
   problem/context/hypothesis/action/result/review chronology. Every attempt
   needs exact predecessor IDs; the typed receipt must cite the result event.
   Retain failures, missing evidence and actual resource measurements.
3. Supply frozen as-of graphs and separate later replay/shadow cases. Model
   captures must identify the exact input and actual model/version/usage.
   Optional local/frontier callbacks return bounded proposals only.
4. Have the independent verifier produce the existing P0 promotion proof for the
   exact candidate revision and both evaluation artifact IDs. This fabric
   cannot authenticate that verifier or grant execution authority.
5. Pass selected proposals through existing AAXP, policy, authority, adapter,
   receipt and verification boundaries. Feed the actual result back using the
   returned proposal-event ID. Preserve unchanged authority through demotion.
6. Persist/export admitted observations through the existing NXC/DKG dispatch
   path. No direct remote-memory client is included in this change.

## Receiving acceptance

Run the source lifecycle and negative tests in `docs/verified-evolution.md`.
Then retain a real authenticated mission trace across proposal, authorized effect,
independent verification and subsequent replay, with denied cross-tenant and
authority-expansion cases. Compare chronological benchmarks only on the same
corpus and grading evidence.

The local historical run has no independent pipeline/repair labels or captured
model answers. Its null accuracy denominators are an evidence gap, not an
activation prerequisite that source tests can manufacture. Sprint deployment and
submission acceptance remain governed by their existing owners and receipts.
