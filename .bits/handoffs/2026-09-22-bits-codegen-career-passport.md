# ─── CGRF Header ───────────────────────────────────────────────
# File:        .bits/handoffs/2026-09-22-bits-codegen-career-passport.md
# Stage:       11_COMMIT
# SRS:         SRS-BUILDANDDO-UPGRADE-001
# CAPS:        pending
# CK:          pending
# Dispatch:    VCC-BUILDANDDO-UPGRADE-001
# Seat:        BITS-CODEGEN
# Owner:       Citadel Nexus Inc.
# Created:     2026-09-22
# Depends:     docs/career-passport.md, .bits/out/VCC-BUILDANDDO-UPGRADE-001/career-passport-report.md, libs/career_passport/authority.py
# EnumType:    Doc
# EnumEdges:   CONSUMES docs/career-passport.md; CONSUMES .bits/out/VCC-BUILDANDDO-UPGRADE-001/career-passport-report.md; CONSUMES libs/career_passport/authority.py
# Intent:      Specify the identity, live-source, independent-review and browser evidence required to complete one BuildAndDo career dogfood application.
# ───────────────────────────────────────────────────────────────

# Career Passport receiving acceptance

From: BITS-CODEGEN. To: IDE1 (native workspace), CMAX-B (permitted browser
runtime), the applicant and an independent verifier. Source authorization is
VCC-BUILDANDDO-UPGRADE-001. A receiving external-write dispatch has not been
provided or created. This document sends no handoff event or application.

The source slice is available at `/app/career` and through
`python -m libs.career_passport`. The real BuildAndDo repository run has 22 exact
artifacts and 19 agent-attributed contributions. It has no independently verified
personal contribution, live job, application or outcome. Repository commit
metadata is available; some ancestor trees needed for diffs are absent. The
collector retained metadata and that explicit gap without fetching objects.
Full Python discovery still reports two errors from unchanged Semantic Twin
history consumers needing those absent trees (one setup error prevents four
compiler cases), plus 32 dependency skips. Supply complete local history for the
receiving regression run; do not disable those existing checks to claim a pass.

Complete these receiving steps in order:

1. Bind the applicant's native account, workspace and canonical person identity.
   Obtain a current scoped export from `/app/career`. The repository-only dogfood
   identifier is deliberately unresolved and cannot serve as this mapping.
2. Inspect source artifacts and actual participation with an independent
   verifier. Preserve agent/team attribution. Establish capability, scope,
   assistance and occurrence time from evidence, then supply the authenticated
   review policy/pins separately from the work bundle. Never grant personal
   implementation because the applicant owns the repository or directed an agent.
3. Confirm target role families, locations/remote arrangements and selected
   employer boards. Capture 100 permitted current public postings and retain
   raw responses, source URLs, timestamps and gaps. This sandbox supplied no
   live search results; synthetic test postings cannot be reused as employer data.
4. Inspect the ten dossiers and three supported packages, resolving extraction
   ambiguity and every DO NOT CLAIM gap. The target is conditional on sufficient
   real evidence and suitable jobs; do not pad the shortlist or generate unsupported
   claims to reach a quota. Resolve the PDF Unicode limitation if it affects the
   real applicant, then recompute the exact package binding.
5. Run the seven new rendered page cases with locked dependencies, native access
   isolation and a permitted browser. Check account/workspace changes, revocation,
   delayed import, labels, keyboard use, small viewports and document downloads.
   The source client uses only the existing authenticated operator read endpoint.
6. For one chosen application, show the applicant the actual documents,
   recipient, current employer form and all answers. Obtain receiving A3
   authorization and scoped J3/J4 approval. Sensitive and binding fields need
   explicit human answers. Stop for CAPTCHA or anti-bot challenges.
7. Implement/activate the receiving browser adapter under that dispatch. Load
   authoritative prior attempts, atomically reserve the attempt before the click,
   and reconcile unknown outcomes against the ATS. Capture the real submission
   receipt, protect private artifacts, and retain separately authenticated outcome
   pins. Observe actual responses/interviews/offers; never seed example metrics.

The public mirror contains the preparation and authority contracts, not an
activated ATS writer, encrypted personal-answer store or durable private outcome
owner. Public ATS retrieval gives no submission API entitlement. Existing
eighteen-profile acceptance and eleven readiness milestones remain required;
this handoff does not mark them passed.
