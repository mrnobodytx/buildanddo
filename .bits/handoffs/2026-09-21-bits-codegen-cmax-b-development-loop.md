# ─── CGRF Header ───────────────────────────────────────────────
# File:        .bits/handoffs/2026-09-21-bits-codegen-cmax-b-development-loop.md
# Stage:       11_COMMIT
# SRS:         SRS-BUILDANDDO-DEVELOPMENT-LOOP-001
# CAPS:        pending
# CK:          pending
# Dispatch:    VCC-BUILDANDDO-DEVELOPMENT-LOOP-001
# Seat:        BITS-CODEGEN
# Owner:       Citadel Nexus Inc.
# Created:     2026-09-21
# Depends:     docs/development-loop.md, .bits/out/VCC-BUILDANDDO-DEVELOPMENT-LOOP-001/report.md, .bits/handoffs/2026-09-21-bits-codegen-cmax-b-capability-runtime.md, docs/submission-guide.md
# EnumType:    Doc
# EnumEdges:   CONSUMES docs/development-loop.md; CONSUMES .bits/out/VCC-BUILDANDDO-DEVELOPMENT-LOOP-001/report.md; EXTENDS .bits/handoffs/2026-09-21-bits-codegen-cmax-b-capability-runtime.md; CONSUMES docs/submission-guide.md
# Intent:      Give receiving owners exact missing runtime, review and submission inputs after the local development connections have been exercised.
# ───────────────────────────────────────────────────────────────

# Development-loop receiving handoff

From BITS-CODEGEN to CMAX-B / IDE1 / COPILOT and the repository owner.
Status: requested; no private execution, seat event, external message, issue,
deployment or reviewer assignment was dispatched by this file.

The additive adapters now connect repository-authenticated GitHub Actions reads,
the existing evolution journal, information assessment, P0/P1 mission packets,
frozen source-test predictions and the existing independently pinned review gate.
The proposed receiving verifier in the dogfood packet is CMAX-B; that field does
not establish an assignment, authenticated identity or completed review.

## Observed evidence

- Actual run `35621244782`, attempt 1, for reference source
  `3aea1500d957803f62d5a04c69749b2c6cdeb383` reports failure with zero job steps.
  The annotation for its job/check `106404539442` states that the job was not
  started because the account is locked due to a billing issue. This cannot be
  interpreted as a test failure, deployment, or DORA acknowledgement.
- The actual local experiment captured 105 semantic objects, proposed one P0
  acceptance mission, selected three source suites and executed 42 tests with
  no failures or skips. Git HEAD supplied reference context; separate hashes
  bind the working-tree bytes, including these new adapters. It is not final
  candidate or deployed acceptance.
- No independently graded pair, discovered candidate or preferred capability
  was supplied by this run. The unchanged registry selected `UNRESOLVED`.
  Full-ladder regression fixtures are synthetic and cannot fill those gaps.
- Existing Day-21 source/audit checks find the closure machinery and consistent
  `https://buildanddo.com` links. No current runtime submission bundle was
  available here. The historical `submissions/hostinger` artifacts were retained.
- Full local acceptance reaches only 3/18 passing profiles: four fail, one is
  held and ten native profiles are blocked. After removing sandbox process
  restrictions, source Node executes 509 tests; 32 fail because the existing
  migration fixtures omit `$filepath`. Source Python executes 688 with zero
  failures but six skips (five need `pypdf`, one needs Discord.py). Frontend
  dependencies and both native PocketBase binaries remain unavailable.

Public source-bound summaries are in this dispatch's `dogfood.json` and
`validation.json`. Full provider responses, the journal, frozen prediction,
test-run log and proposed mission packet remain local under `/tmp` in this
session. Reacquire/export them into authorized receiving storage before the
session expires; a content digest cannot replace absent bytes or authentication.
`dogfood.py` reproduces the experiment from a newly selected real provider
capture without generating labels or independent receipts.

## Required receiving work

| Owner | Bounded action | Evidence needed |
|---|---|---|
| Repository owner | Resolve the Actions billing block and rerun the selected candidate | Actual run/attempt/job/step observations; separately inspect any hosting build failure |
| IDE1 / test owner | Repair the existing migration fixtures under the upgrade authority | Reproduce `$filepath` failures in business-execution, business-learning and government-learning suites; implement the declared PocketBase host API in their fixtures and rerun the source gate |
| CMAX-B / IDE1 | Bind authorized development exporters and the existing journal | Receiving dispatch, repository/tenant scope, authenticated exporter identity, source revision, observation times and retained bytes |
| Independent verifier / COPILOT | Grade the actual prediction and process result | Reviewer-authored `OutcomeLabels`, exact `VerificationReceipt`, separately distributed `ReviewPolicy` verifier/receipt pins and the `development_outcome` check |
| CMAX-B | Qualify change-impact/test selection through the existing ladder | Two successful discovery examples, ten disjoint replay cases, five shadow cases, measured safety/recall, real teacher captures, known SBOM/source/context compatibility and independent TEVV/`PromotionProof` |
| IDE1 / acceptance operator | Restore the locked frontend toolchain and both native profiles | Installed dependencies; disposable PocketBase package 0.39.8 and compose 0.28.4 profiles; actual check counts/logs and required artifacts |
| Private release owner | Select and deploy the final accepted merged candidate | Exact SHA, artifact digest, release/provider readback and public URL receipt from the existing governed lane |
| Acceptance operator / independent verifier | Capture the actual business journey | Browser evidence, native execution output, separate verification, operator readback and nonsynthetic replay bound to that candidate |
| Repository owner | Complete the Hostinger entry evidence | Meaningful use of all four products, six required materials, actual participant-only instructions, official-rule capture and explicit candidate-bound owner review |

Runtime versions above must be rechecked against `apps/pocketbase/.pocketbase-version`
and `docker-compose.yml` when provisioning. The final report retains the versions
actually requested by the acceptance runner. No runtime is installed by this
handoff.

Five written materials are now prepared in this dispatch's `submission-draft.md`:
product summary, evaluator directions, current license/attribution posture,
privacy/permissions and rollback/support. They are source-backed drafts. The
actual recorded walkthrough remains missing; the source video shot list is not
a recording. The existing submission auditor still returns HOLD after the five
dated material references are supplied. Review the copy against the accepted
product and actual participant form before publication.

Run `scripts/ci/hostinger_readiness.py`, `scripts/ci/day21_submission.py audit`
and `scripts/ci/submission_readiness.py` against the final selected candidate.
Do not rebind this run's local evidence to a later SHA or substitute an old
12/12 probe for full acceptance. `READY_FOR_OWNER_SUBMISSION` remains conditional
on the actual evidence; the owner then performs the external submission.

## Preserve authority and the sprint freeze

The generated SRS/dispatch/registry material is `proposed`. Review and dispatch
existing A1/A2 work through the receiving governance path before implementation;
source prose or a priority score never approves an action. Keep public claims
attributed, resolve stale/copied/conflicting inputs, and prioritize acceptance,
demo clarity and product proofs. New ontologies, providers and marketplaces
remain deferred. Private NXC/DKG ingestion, AAXP execution and publication stay
under their receiving dispatches.

Rollback removes the additive adapters and their registration. Preserve journal
history, failed attempts and reviewer receipts. No remote state needs reversing.
