# ─── CGRF Header ───────────────────────────────────────────────
# File:        .bits/srs/SRS-BUILDANDDO-DEVELOPMENT-LOOP-001.md
# Stage:       04_HYPOTHESIZE
# SRS:         SRS-BUILDANDDO-DEVELOPMENT-LOOP-001
# CAPS:        pending
# CK:          pending
# Dispatch:    VCC-BUILDANDDO-DEVELOPMENT-LOOP-001
# Seat:        BITS-CODEGEN
# Owner:       Citadel Nexus Inc.
# Created:     2026-09-21
# Depends:     AGENTS.md, libs/evolution, libs/capability_tokens/verification.py
# EnumType:    Doc
# EnumEdges:   CONSUMES AGENTS.md; EXTENDS libs/evolution; CONSUMES libs/capability_tokens/verification.py
# Intent:      Connect existing learning and mission boundaries to actual development evidence without manufacturing qualification or submission readiness.
# ───────────────────────────────────────────────────────────────

# SRS-BUILDANDDO-DEVELOPMENT-LOOP-001

Status: in_progress. Risk: A1. Seat: BITS-CODEGEN.

The owner's September 21 continuation prioritizes the missing connections over
new architecture. The AGENTS.md additive fast path authorizes this registration,
dispatch and new adapters/tests/docs before implementation. Preserve the existing
evolution, capability-token, semantic-twin and submission gates. Required context
and readiness locks are regenerated after source review. The session controls
the branch name.

## Bounded outcome

1. Read actual GitHub Actions observations using the configured repository CLI.
   Bind repository, run attempt, source SHA, job steps, capture time and content.
   Persist observations in the existing evolution journal. A failed, skipped,
   cancelled or empty job is not a completed test or independent verification.
   Replaying saved bytes does not reauthenticate their source.
2. Assess authorized information as data with provenance, freshness, common
   origins, conflicts and suspicious-instruction flags. Competitor assertions
   remain attributed claims. Express opportunity links in the existing semantic
   vocabulary. Rank bounded P0/P1 proposals using explicit heuristic inputs and
   the owner's five judging weights; defer architecture expansion.
3. Compile proposals into reviewable SRS/dispatch/mission packets with distinct
   builder/verifier identities and an existing ActionProposal. Proposed A2/A3
   work cannot self-register, approve, execute or create remote issues.
4. Freeze a change-impact/test-selection prediction before running a bounded
   set of existing source tests. Retain exact source/context, test counts,
   skips, failures, timestamps and logs. Require separately pinned independent
   receipts before labels enter the existing replay and promotion machinery.
   No outcome labels are inferred from the predictor's selected test list.
5. Dogfood actual repository observations and local source-test execution.
   Exercise existing qualification gates; insufficient discovery, holdout,
   shadow or independent TEVV remains HOLD. Audit submission using the existing
   acceptance and submission tools and retain the specific missing inputs.

## Excluded effects

No new service, ontology, marketplace, provider credentials, remote mutation,
private-plane execution, signing, deployment, owner approval or competition
submission. No synthetic examples count as real qualification evidence. Selecting
an unaccepted local revision does not establish a final submission candidate.

## Verification

- Focused standard-library tests cover source scope/attempt mismatches, malicious
  input as inert data, stale/copied/conflicting claims, authority preservation,
  prediction chronology, exact source/log bindings and untrusted review denial.
- Existing evolution, capability-token and semantic-twin regression suites pass.
- Strict mypy and Ruff pass for the adapters; stdlib trace measures at least
  80 percent executable-line coverage per new production module.
- Public-boundary, measured-context and both readiness contract checks pass.
- Real captures preserve missing labels and acceptance blockers explicitly.

## Rollback

Remove the additive adapters and new registration. Retain captured observations,
local journal history and failed attempts. No external system is changed.
