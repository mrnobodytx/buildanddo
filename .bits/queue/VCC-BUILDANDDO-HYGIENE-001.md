# ─── CGRF Header ───────────────────────────────────────────────
# File:        .bits/queue/VCC-BUILDANDDO-HYGIENE-001.md
# Stage:       11_COMMIT
# SRS:         SRS-BUILDANDDO-HYGIENE-001
# CAPS:        pending
# CK:          pending
# Dispatch:    VCC-BUILDANDDO-HYGIENE-001
# Seat:        BITS-CODEGEN
# Owner:       Citadel Nexus Inc.
# Created:     2026-09-24
# Depends:     .bits/srs/SRS-BUILDANDDO-HYGIENE-001.md, .bits/srs_registry.yml
# EnumType:    Doc
# EnumEdges:   GATES claude/* and bits/SRS-BUILDANDDO-HYGIENE-001-* branches;
#              DEPENDS_ON .bits/srs/SRS-BUILDANDDO-HYGIENE-001.md
# DAG Node:    none
# Intent:      Record the owner's request to verify the unused/scattered audit and improve the
#              system, without deployment, secret or external effects.
# ───────────────────────────────────────────────────────────

# Dispatch VCC-BUILDANDDO-HYGIENE-001

**SRS:** SRS-BUILDANDDO-HYGIENE-001 **Risk:** A2 **Seat:** BITS-CODEGEN **Status:** in_progress

Owner request, 2026-09-24: "dig deeper, make sure your findings are accurate, then improve the
system", following an audit for scattered or unused sections. The changes touch shared CI, governance
tooling and web dependencies, so they are A2. This dispatch records the request and does not
self-authorize anything beyond it.

## Objective

Every finding the audit verified is either fixed or recorded in the SRS. The tools that should have
caught them (the redaction scan, the context lock, knip) now report the truth.

## Task table

| # | Task | Gate command | Status |
|---|------|--------------|--------|
| 1 | Machine names out of public docs; docs gated by the redaction scan | `python scripts/ci/public_redaction.py scan docs README.md CHANGELOG.md CLAUDE.md AGENTS.md CONTRIBUTING.md && echo PASS` | done |
| 2 | Context lock names GitHub-run gates; new governance findings with tests | `python -m unittest tests.upgrade.test_hygiene && python scripts/ci/agent_context.py --check && echo PASS` | done |
| 3 | Governance data: delivered dispatch removed, specs registered, `.tech` corrected | `python scripts/ci/agent_context.py --check && echo PASS` | done |
| 4 | Dead web code and unused dependencies removed; knip.json accurate | `cd apps/web && npm run lint && npm run build && npx vitest run && echo PASS` | done |
| 5 | Locks, readiness, boundary | `python scripts/ci/hostinger_readiness.py --check && python scripts/ci/system_growth.py --check && python scripts/ci/verify_public_boundary.py && echo PASS` | done |

## Constraints

- May touch: the two scrubbed docs, `scripts/ci/agent_context.py`, `scripts/ci/system_growth.py`,
  a new `tests/upgrade/test_hygiene.py`, `.github/workflows/pr-governance.yml` and
  `.gitlab-ci.yml` (one step each), `.bits/srs_registry.yml`, the delivered dispatch (delete only),
  `.bits/srs/SRS-BUILDANDDO-DAY21-CLOSURE-001.md` (a dated correction line only), dead files under
  `apps/web/src` (delete only), the stale mock in
  `apps/web/src/components/workspace/__tests__/TutorialCatalog.test.jsx`, `apps/web/package.json`,
  `package-lock.json` (regenerated by npm, never by hand), `knip.json`, `README.md`, this dispatch,
  the SRS, and the `.bits` locks.
- Must not touch: any SRS status other than registering the 3 unregistered specs; Python tools other
  seats may run; migrations; deployment execution; secrets.
