# SectionLabel

The uppercase red kicker above a headline, e.g. *Learn together*.

Ported from `site/ui.jsx`. **Consumer provides:** `children`, optional `icon`. One per section, directly above a `display` headline.
