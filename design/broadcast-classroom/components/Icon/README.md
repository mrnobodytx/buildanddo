# Icon

Inline Lucide icon (v0.469.0, the version the app pins), 2px stroke, inheriting `currentColor`.

**Consumer provides:** `name` (see the Icons group), optional `size` (default 20). Icons are decorative (`aria-hidden`); the adjacent word carries meaning.
