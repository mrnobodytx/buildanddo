# Card

A panel on `card` with a 1px `border` rule, square, no shadow; the container for the shared lesson, attendance and discussion.

Ported from `site/ui.jsx`. **Consumer provides:** children, optional `padding` (`md` 20px default · `lg` 24px).
