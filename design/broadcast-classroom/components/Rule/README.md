# Rule

Editorial rules: thin (1px `border`) between items, thick (2px `foreground`) under a masthead, double (3px double) for major divisions.

Ported from `site/ui.jsx`. **Consumer provides:** `thick` or `double` flags. Rules divide; cards contain. Never use a shadow in place of either.
